{
  "name": "sales-team-top-100",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build.js",
    "build:csv": "node scripts/build.js --csv",
    "discover": "node scripts/discover.js",
    "serve": "npx --yes serve ."
  }
}
